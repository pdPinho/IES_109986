# Adapted ex2 to this exercise, hence why some functions aren't exactly needed (such as getting movie by ID)

# Usage
mvn clean package
docker-compose up
docker-compose down
docker-compose up

(Devido à base de dados demorar muito (não sei porquê), o docker não funciona na PRIMEIRA vez)
(Mas tentando novamente aquilo já funciona...)